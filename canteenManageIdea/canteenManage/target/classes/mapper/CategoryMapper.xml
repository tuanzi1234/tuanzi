<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CategoryMapper">

<!--    SELECT * FROM category WHERE title LIKE '%{title}%' or content LIKE '%{content}%' -->
   <select id="selectAll" resultType="com.example.entity.Category">
        select * from `category`
        <where>
            <if test="title != null"> and title like concat('%', #{title}, '%')</if>
            <if test="content != null"> and content like concat('%', #{content}, '%')</if>
        </where>
    </select>

    <!-- insert into category (id, title, ...) values ('category', 'category', ...) -->
    <insert id="insert" parameterType="com.example.entity.Category" useGeneratedKeys="true">
        insert into `category`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="title != null">title,</if>
            <if test="content != null">content,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="title != null">#{title},</if>
            <if test="content != null">#{content},</if>
        </trim>
    </insert>


    <!--    UPDATE category SET title = 'new_title', content = 'new_content' WHERE id = #{id} -->
    <update id="updateById" parameterType="com.example.entity.Category">
        update `category`
        <set>
            <if test="title!= null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
        </set>
        where id = #{id}
    </update>
</mapper>